# Core framework
Django==5.2.8
django-extensions==4.1
django-otp==1.6.0
djangorestframework==3.17.1

# Data import/export and document generation
openpyxl==3.1.5
pandas==3.0.2
polib==1.2.0
python-barcode==0.15.1
qrcode==8.2
reportlab==4.4.1
pydyf==0.11.0
weasyprint==62.3
xlsxwriter==3.2.9

# Imaging and security helpers
pillow==12.1.1
pyotp==2.9.0
cryptography==50.0.1  # Fernet field encryption for stored mailbox/SMTP credentials

# Production deployment (Ubuntu 24.04 LTS; Gunicorn behind Nginx)
# gunicorn is Unix-only - the environment marker keeps Windows dev installs working.
gunicorn==23.0.0 ; sys_platform != 'win32'
# PostgreSQL driver (production database; SQLite remains the dev default)
psycopg2-binary==2.9.12
# Static files: WhiteNoise serves + compresses static assets (manifest cache-busting in prod)
whitenoise==6.12.0
# Caching: Redis via django-redis (enabled when DJANGO_REDIS_CACHE_URL is set)
django-redis==5.4.0
redis==8.1.0